PROGRAM `example` WITH ARGS:
  `input`
  `output`
AND VARIABLES:
  DECLARATIONS WITH TYPE INTEGER:
    `x`
    `y`
AND SUBPROGRAMS:
  FUNCTION `gcd` WITH ARGS:
    DECLARATIONS WITH TYPE INTEGER:
      `a`
      `b`
  RETURNING INTEGER
  AND VARIABLES
  DOES:
    IF FOLLOWING IS TRUE:
      BINOP EQ
        LEFT:
          `b`
        RIGHT:
          LIT `0`
    THEN:
      ASSIGN `gcd` TO:
        `a`
    OTHERWISE:
      ASSIGN `gcd` TO:
        APPLY `gcd` TO ARGS:
          `b`
          BINOP MOD
            LEFT:
              `a`
            RIGHT:
              `b`
DOES:
  APPLY `read` WITH:
    `x`
    `y`
  APPLY `write` WITH:
    APPLY `gcd` TO ARGS:
      `x`
      `y`
-- done dumping ast --
